<h4>Leave a comment:</h4>
<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :name, "Name" %><br>
    <%= f.text_area :name %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </p>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>