<h4>Leave a comment</h4>
<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.text_field :name, placeholder: "Name" %>
  </p>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>